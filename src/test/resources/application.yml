spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: test-data.sql
    schema: test-schema.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  shell:
    interactive:
      enabled: false